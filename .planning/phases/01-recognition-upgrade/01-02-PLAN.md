---
phase: 01-recognition-upgrade
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - scripts/download_models.sh
autonomous: false
requirements: [RECG-02]

must_haves:
  truths:
    - "Download script fetches base.en-q5_1, small.en-q5_1, and medium.en-q5_0 models"
    - "Full project builds successfully with whisper.cpp v1.8.3 and Vulkan enabled"
    - "All existing tests pass with the upgraded whisper.cpp"
    - "User can speak a test phrase and observe improved transcription accuracy"
  artifacts:
    - path: "scripts/download_models.sh"
      provides: "Multi-model download with q5_1/q5_0 quantized variants"
      contains: "small.en-q5_1"
  key_links:
    - from: "scripts/download_models.sh"
      to: "huggingface.co/ggerganov/whisper.cpp"
      via: "curl download of ggml model files"
      pattern: "huggingface.*ggml.*q5"
---

<objective>
Update the model download script for quantized multi-model support, verify the full build compiles with whisper.cpp v1.8.3 + Vulkan, and validate accuracy improvement via manual testing.

Purpose: RECG-02 -- confirm the upgrade delivers measurable accuracy improvement and all models are downloadable.
Output: Updated download script, verified build, user-confirmed accuracy improvement.
</objective>

<execution_context>
@/home/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/home/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recognition-upgrade/01-CONTEXT.md
@.planning/phases/01-recognition-upgrade/01-RESEARCH.md
@.planning/phases/01-recognition-upgrade/01-01-SUMMARY.md

@scripts/download_models.sh

<interfaces>
<!-- From 01-RESEARCH.md - model download URLs and filenames -->
HuggingFace base URL: https://huggingface.co/ggerganov/whisper.cpp/resolve/main
Model files:
  - ggml-base.en-q5_1.bin
  - ggml-small.en-q5_1.bin
  - ggml-medium.en-q5_0.bin (no q5_1 for medium -- only q5_0 available)

<!-- From config/default_config.json (after Plan 01) -->
Default model: "small.en-q5_1" -> resolves to ggml-small.en-q5_1.bin
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update download script for quantized multi-model support and verify build</name>
  <files>scripts/download_models.sh</files>
  <action>
Rewrite the Whisper section of `scripts/download_models.sh` to download all three English model sizes with quantization:

1. Replace the single-model Whisper download section (lines 79-119) with a multi-model loop:
   - Define an associative array of models to download:
     - `base.en-q5_1` -> `ggml-base.en-q5_1.bin`
     - `small.en-q5_1` -> `ggml-small.en-q5_1.bin`
     - `medium.en-q5_0` -> `ggml-medium.en-q5_0.bin` (NOTE: medium.en has NO q5_1 variant on HuggingFace, use q5_0)
   - Base URL: `https://huggingface.co/ggerganov/whisper.cpp/resolve/main`
   - For each model: skip if file already exists (check file size > 0), download with curl -L, report success/failure
   - Remove the old ETag-based update logic (overkill for multiple binary files; simple existence check is sufficient)
   - Keep the Vosk section unchanged

2. After writing the script, verify the full project build:
   - Run `cmake -B build -DCMAKE_BUILD_TYPE=Debug` (this will fetch whisper.cpp v1.8.3 from ggml-org)
   - Run `cmake --build build -j$(nproc)` (full build including Vulkan backend)
   - Run `cd build && ctest --output-on-failure` (all tests including new test_whisper_refinement)
   - If build fails due to whisper.cpp v1.8.3 API changes, fix the compilation issues in the files from Plan 01

Note: The build step will take time because it downloads and compiles whisper.cpp v1.8.3 from source. This is expected.
  </action>
  <verify>
    <automated>cd /home/ryan/repos/verbal-code && bash -n scripts/download_models.sh && echo "Script syntax OK" && grep -q "small.en-q5_1" scripts/download_models.sh && grep -q "medium.en-q5_0" scripts/download_models.sh && grep -q "base.en-q5_1" scripts/download_models.sh && echo "All models present" && cmake -B build -DCMAKE_BUILD_TYPE=Debug 2>&1 | tail -5 && cmake --build build -j$(nproc) 2>&1 | tail -10 && cd build && ctest --output-on-failure 2>&1 | tail -20</automated>
  </verify>
  <done>Download script fetches all three quantized English models. Project builds cleanly with whisper.cpp v1.8.3 and GGML_VULKAN=ON. All existing and new tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify accuracy improvement with upgraded model</name>
  <files>none</files>
  <action>
Human verification checkpoint. No automated action -- user tests the full upgrade end-to-end.
  </action>
  <verify>User confirms accuracy is acceptable</verify>
  <done>User has approved transcription accuracy with the upgraded whisper.cpp v1.8.3 and small.en-q5_1 model</done>
  <what-built>
Upgraded whisper.cpp from v1.7.3 to v1.8.3 with Vulkan GPU acceleration, updated default model from base.en to small.en-q5_1, wired initial_prompt support, and auto-detect thread count.
  </what-built>
  <how-to-verify>
1. Download the new models:
   ```bash
   bash scripts/download_models.sh
   ```
   Verify all three models download (base.en-q5_1, small.en-q5_1, medium.en-q5_0).

2. Build and run the application:
   ```bash
   cmake -B build -DCMAKE_BUILD_TYPE=Release
   cmake --build build -j$(nproc)
   ./build/verbal-code
   ```

3. Speak several test phrases using push-to-talk. Observe:
   - Does the application start without errors? (check terminal output for "Whisper model loaded" log)
   - Is transcription noticeably more accurate than before the upgrade?
   - Does the Whisper log show GPU backend selection? (look for ggml backend messages)

4. Try technical phrases with uncommon words to stress-test accuracy:
   - "Initialize the Kubernetes deployment with three replicas"
   - "The PostgreSQL query uses a lateral join"
   - Your own domain-specific phrases

Expected: Application starts, loads small.en-q5_1 model, transcription is visibly more accurate than the old base.en model.
  </how-to-verify>
  <resume-signal>Type "approved" if accuracy is acceptable, or describe issues observed</resume-signal>
</task>

</tasks>

<verification>
- `bash -n scripts/download_models.sh` -- script has valid syntax
- Script references all three quantized model variants
- Full build succeeds: `cmake -B build && cmake --build build -j$(nproc)`
- All tests pass: `cd build && ctest --output-on-failure`
- User confirms accuracy improvement via manual testing
</verification>

<success_criteria>
- Download script fetches base.en-q5_1, small.en-q5_1, and medium.en-q5_0 models from HuggingFace
- Project builds successfully with whisper.cpp v1.8.3 and Vulkan backend compiled in
- All existing tests continue to pass alongside new whisper refinement tests
- User has manually verified that transcription accuracy is improved over the v1.7.3 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/01-recognition-upgrade/01-02-SUMMARY.md`
</output>
